import styled from "styled-components";

const HeaderMainWrap = styled("header")`
  display: flex;
  flex-direction: column;
  width: 100%;
`;

export const useHeaderMainWrapStyle = () => ({
  HeaderMainWrap,
});
